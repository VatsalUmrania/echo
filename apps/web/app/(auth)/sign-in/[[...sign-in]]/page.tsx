import SignInView from '@/modules/auth/ui/views/sign-in-view'

/**
 * Page component for the sign-in route.
 *
 * Renders the SignInView UI for user authentication.
 *
 * @returns The JSX element containing the sign-in view.
 */
export default function Page() {
  return <SignInView />
}