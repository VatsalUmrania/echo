import SignUpView from '@/modules/auth/ui/views/sign-up-view'

/**
 * Page component that renders the sign-up view.
 *
 * This default-exported React component simply returns the project's SignUpView.
 * It accepts no props and has no side effects.
 *
 * @returns The SignUpView JSX element for the sign-up route.
 */
export default function Page() {
  return <SignUpView />
}